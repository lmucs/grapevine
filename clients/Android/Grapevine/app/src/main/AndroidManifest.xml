<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.lmu.grapevine" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/grapevine_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterUser"
            android:label="@string/title_activity_create_new_account"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="cs.lmu.grapevine.MainActivity" />
        </activity>
        <activity
            android:name=".FeedEvents"
            android:label="@string/event_feed" >
        </activity>
        <activity
            android:name=".CalendarView"
            android:label="@string/calendar_view"
            android:parentActivityName=".FeedEvents" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="cs.lmu.grapevine.FeedEvents" />
        </activity>
        <activity
            android:name=".ViewEvent"
            android:label="@string/title_activity_view_event"
            android:parentActivityName=".FeedEvents" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="cs.lmu.grapevine.FeedEvents" />
        </activity>
    </application>

</manifest>
